# WWriting Codex 记忆

## Superpowers 使用方式

除非用户在当前消息中主动点名某个 superpowers skill，否则不要自动启动完整的 superpowers 流程，也不要因为任务看起来可能适用某个 skill 就强制加载它。用户主动调用时，才按该 skill 的完整要求执行；未主动调用时，按任务本身和本文件的其他规则处理。

## 用户需求理解：先当产品经理，再当工程师

用户可能不懂技术，需求描述可能带情绪、口语化、跳跃、不完整，也未必会使用专业术语。遇到这类输入时，不要要求用户先把技术细节说清楚；先用产品经理视角理解真实意图，把自然语言需求翻译成“目标、使用场景、当前痛点、期望结果、验收标准”。

执行规则：

- 先提炼用户真正想完成什么，而不是纠结用户说法是否专业。
- 对模糊需求，先给出自己的理解和可执行拆解；只有存在会影响方向的关键不确定性时，才问少量必要问题。
- 不要把用户的急躁、抱怨或粗口当成对抗信号；保持冷静、直接、简明，推动问题解决。
- 面向非技术用户解释时，用产品语言和可感知结果，少用内部实现术语；必须提技术细节时，说明它对用户体验的影响。
- 开始实现前，尽量把需求落成可验证的结果，例如“点击后出现设置弹窗”“桌面快捷方式打开的是新版应用”“保存后重启仍保留内容”。
- 如果用户表达的是问题现象，优先还原用户路径和预期体验，再进入代码、日志、测试层面的排查。

## 用户偏好：API Key 和模型配置要本地持久、可确认、可复用

用户不喜欢当前 API Key 保存后仍看不到完整内容、数据库里像是没有保存任何东西的体验。这个软件定位是本地化的智能体写作工具，用户不会把自己的配置共享给别人；其他用户也应该保存各自的本地 key。因此在这个项目里，不要把 key 的保护优先级想得过高，不能为了“安全感”牺牲本地可用性、可确认性和跨项目复用。

期望体验：

- 保存 API Key 和模型配置后，重启应用仍能看到并确认完整配置确实存在。
- 设置里的 key/模型配置应该真正写入本地持久存储，不要出现保存后数据库仍为空、下次打开又像没配置过的状态。
- 打开新项目或选择项目目录时，应能直接选择设置里已经配置好的模型，不需要重新输入 key 或重新配置供应商。
- 每次启动应用后，用户不应该为了确认模型是否真的配置好而被迫再次进入设置重新输入。
- 在本地个人使用语境下，优先保障“我能看见完整 key、能确认它保存了、能跨项目复用”，再考虑遮罩、隐藏或额外保护。

## 高频回归：按钮看得到但点不动

这个项目反复出现过“设置”“新建小说”等按钮无法点击的问题。不要把它当成单个按钮漏绑事件处理；先按模块启动和点击命中链路排查。

已确认过的根因类型：

- 前端模块没有执行：`app.js` 的 ESM 依赖加载失败会导致动态导航不渲染、事件监听不绑定。典型案例是 `components/failure-card.js` import `../../shared/failure-commands.mjs`，但静态服务器没有服务 `/shared/*.mjs`。
- 静态资源 MIME 错误：`.mjs` 必须以 `text/javascript` 返回，否则浏览器/Electron 会拒绝模块。
- 原生标题栏或拖拽区域覆盖：`titleBarOverlay`、整块 `.topbar`/`.rail-top` 的 drag region 会吃掉点击。
- 浮层拦截：toast、scrim、隐藏 modal 如果保留 `pointer-events: auto`，会挡住后面的按钮。
- 测试选择器漂移：UI 改版后不要只改测试绕开问题；真实可见按钮必须能收到 trusted pointer click，并产生预期 UI 状态。

修 UI、Electron、静态服务、打包配置后，必须至少跑：

```powershell
npm run verify:app-clickability
npm run verify:app-shell
npm run verify:desktop-shell
```

准备交付给桌面快捷方式使用前，必须跑：

```powershell
npm run verify:local
```

`verify:app-clickability` 是关键防线：它会启动真实 Electron 窗口，逐个点击刷新、隐私、左侧导航、新建小说、创建弹窗、设置弹窗、API Key 控件、联网开关、章节抽屉、阅读器、资料按钮、命令栏等路径。不要用“代码看起来绑了事件”替代这项验证。

桌面快捷方式通常指向：

```text
D:\WWriting\dist-desktop\win-unpacked\WWriting Novel Agent.exe
```

源码修复后如果没有重新打包，桌面快捷方式打开的仍可能是旧 exe。

## 应用图标资产

WWriting 当前确认使用的桌面应用图标已经存入项目：

```text
D:\WWriting\src\assets\app-icon.ico
```

资产来源是用户提供的 `13072026_favicon.ico`，原始下载目录只作为来源记录，后续使用必须以项目内文件为准，不能依赖 `Downloads` 目录。当前文件 SHA-256：

```text
1727D1646F429A1B0F21147C94145B42BF050706CC99EEEDB0FE9E0A5055D323
```

`package.json` 的 `build.icon` 和 `build.win.icon` 均引用该文件。不要随意运行 `npm run assets:icon`，该脚本会重新生成并覆盖当前图标；只有用户明确要求更换图标时才允许覆盖。

Windows EXE 图标会嵌入打包产物。替换源码中的 ICO 后必须重新打包，旧的 `debug\WWriting-Debug.exe` 不会自动更新图标。
