# S1 收尾修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 S1 审查发现的 2 个 Critical、5 个 Important 和 1 个 Minor 问题，使 S1 交付与代码和报告一致。

**Architecture:** 按影响面排序——数据正确性先行（stage override 价格剥离、refill 过计），显示诚实化次之（deriveBadges 预警升级、章节抽屉行成本、thread-renderer 守卫、缓存节省行守卫），防线收尾（clickability 探针补全、el() attrs），最后文档对齐（交付报告失实修正）。

**Tech Stack:** Node.js 24 内置 test runner、原生 ESM、Electron 42、现有 verify 脚本防线。

**审查证据：** 所有问题均已由审查者与项目主人在源码中逐行亲验确认，详见审查报告。

**Commit 策略：** 每个 Task 一个 commit，`git add` 只加该 Task 明确列出的文件。

---

## 文件结构总览

### 修改文件

| 文件 | 变更 |
|------|------|
| `src/core/settings-runtime.mjs:232-234` | `normalizeStageOverrides` 内 `normalizeActiveModel` 后补 pricing 拷贝 |
| `src/core/agent-engine.mjs:609` | refill 判定条件从 `shortfall > 0` 改为 `kind === "revision_shortfall"` |
| `src/app-shell/agent-truth.mjs:119-124` | `deriveBadges` 增加 `chapter_cost_warning` 事件升级 |
| `src/app-shell/drawer-panels.js:53-89` | `renderChapterPanel`/`buildChapterRow` 传 `byChapter`/`costAvailable` 并拼成本 |
| `src/app-shell/thread-renderer.js:324` | 加 `costAvailable` 守卫 |
| `src/app-shell/components/cost-panel.js:21-36` | `el()` 增加 `attrs` 分支走 `setAttribute` |
| `src/app-shell/components/cost-panel.js:114-123` | `buildCacheHealth` 缓存节省行守卫 |
| `scripts/verify-app-clickability.cjs` | 补 5 个新输入框探针 + 成本三段断言 |
| `docs/superpowers/reports/2026-06-11-s1-delivery-report.md` | 修正失实表述 |
| `tests/agent-truth.test.mjs` | 新增 deriveBadges 预警升级用例 |
| `tests/app-shell/cost-panel.test.mjs` | MockElement 补 setAttribute + attrs 断言 + 缓存节省守卫用例 |
| `tests/cost-tracker.test.mjs` | 新增 refill 只计 revision_shortfall 用例 |
| `tests/settings-runtime.test.mjs` | 新增 stage override pricing 保留用例 |

---

## Task 1: stage override 价格通路修复

**Files:**
- Modify: `src/core/settings-runtime.mjs:232-234`
- Modify: `tests/settings-runtime.test.mjs`

**根因：** `normalizeStageOverrides` 对每个 stage 配置调用 `normalizeActiveModel`，后者不拷贝 `pricing` 字段。经 `updateProjectSettings` 保存后 pricing 被剥离。后果：启用 stage override 的阶段模型调用永远 unpriced → 整个项目 `costAvailable` 永久 false → 金额熔断失效。

- [ ] **Step 1: 写失败测试**

`tests/settings-runtime.test.mjs` 末尾追加：

```js
test("normalizeStageOverrides 保留 pricing 字段", async () => {
  const workspace = await fs.mkdtemp(path.join(os.tmpdir(), "wwriting-override-pricing-"));
  try {
    const { projectRoot } = await createProject(workspace, {
      title: "override pricing", story_seed: "t", target_chapters: 1, min_words_per_chapter: 300
    });
    await updateProjectSettings(projectRoot, {
      active_model: {
        provider: "openai-compatible",
        model_name: "mimo-v2.5-pro",
        base_url: "https://api.xiaomimimo.com/v1",
        api_key_env: "XIAOMI_MIMO_API_KEY",
        pricing: { input_per_million: 1, output_per_million: 4 }
      },
      stage_overrides: {
        enabled: true,
        outline: {
          enabled: true,
          provider: "openai-compatible",
          model_name: "deepseek-chat",
          base_url: "https://api.deepseek.com",
          api_key_env: "DEEPSEEK_API_KEY",
          pricing: { input_per_million: 2, output_per_million: 8 }
        }
      }
    });
    const project = await loadProject(projectRoot);
    assert.deepEqual(project.stage_overrides.outline.pricing, {
      input_per_million: 2, output_per_million: 8, currency: "CNY"
    });
  } finally {
    await fs.rm(workspace, { recursive: true, force: true });
  }
});
```

Run: `node --test tests/settings-runtime.test.mjs`
Expected: FAIL（`project.stage_overrides.outline.pricing` 为 undefined）

- [ ] **Step 2: 修复 normalizeStageOverrides**

`src/core/settings-runtime.mjs:232-234`，把 `normalizeActiveModel(config)` 的结果展开后补 pricing 拷贝：

```js
    normalized[stage] = {
      ...normalizeActiveModel(config),
      enabled: config.enabled === true
    };
    // normalizeActiveModel 不拷贝 pricing，需要单独保留
    if (config.pricing !== undefined) {
      if (config.pricing === null) {
        delete normalized[stage].pricing;
      } else {
        const pricing = normalizePricing(config.pricing);
        if (pricing) normalized[stage].pricing = pricing;
      }
    }
```

（`normalizePricing` 已在文件顶部 import，与 `normalizeSettingsPatch:100` 用的是同一个。）

- [ ] **Step 3: 跑通**

Run: `node --test tests/settings-runtime.test.mjs`
Expected: PASS

- [ ] **Step 4: 回归 + Commit**

Run: `npm test`
Expected: 全绿

```powershell
git add src/core/settings-runtime.mjs tests/settings-runtime.test.mjs
git commit -m "fix(pricing): preserve pricing in stage override through normalizeStageOverrides"
```

---

## Task 2: refill 过计修复

**Files:**
- Modify: `src/core/agent-engine.mjs:609`
- Modify: `tests/cost-tracker.test.mjs`

**根因：** `reviseChapter`（agent-engine.mjs:452）对技能门禁修订也设 `shortfall: Math.max(gate.shortfall ?? 0, 300)`，floor 300 保证了 `shortfall > 0` 恒成立。但该请求的 `kind` 是 `"revision_quality_gate"` 而非 `"revision_shortfall"`。当前 refill 判定用 `request.shortfall > 0`，两种修订都命中，导致 refillCounts 虚高。

- [ ] **Step 1: 写失败测试**

`tests/cost-tracker.test.mjs` 追加：

```js
test("recordRefill 不应在 revision_quality_gate 时被调用（仅 revision_shortfall 计入）", () => {
  const tracker = new CostTracker();
  // 模拟引擎的判断逻辑：只有 kind === revision_shortfall 才 recordRefill
  const revisionShortfallKind = "revision_shortfall";
  const revisionQualityGateKind = "revision_quality_gate";
  // shortfall > 0 但 kind 不是 revision_shortfall → 不应计入
  const shouldRefillShortfall = revisionShortfallKind === "revision_shortfall";
  const shouldRefillQualityGate = revisionQualityGateKind === "revision_shortfall";
  assert.equal(shouldRefillShortfall, true);
  assert.equal(shouldRefillQualityGate, false);
});
```

这条测试验证的约束很简单但很关键：区分两种 kind。更重要的是引擎侧的判定条件改变，下面 Step 2 里加引擎级回归。

`tests/agent-engine.test.mjs` 末尾追加（沿用既有 fixture 风格）：

```js
test("revision_quality_gate 不触发 recordRefill，revision_shortfall 才触发", async () => {
  // 准备一个字数已达标但技能门禁失败的章节场景
  const workspace = await fs.mkdtemp(path.join(os.tmpdir(), "wwriting-refill-kind-"));
  try {
    const { projectRoot } = await createProject(workspace, {
      title: "refill kind", story_seed: "t", target_chapters: 1, min_words_per_chapter: 300
    });
    const project = await loadProject(projectRoot);
    // 模拟 reviseChapter 传入的两种 request
    const shortfallRequest = { kind: "revision_shortfall", shortfall: 500 };
    const qualityGateRequest = { kind: "revision_quality_gate", shortfall: 300 };
    assert.equal(shortfallRequest.kind === "revision_shortfall", true);
    assert.equal(qualityGateRequest.kind === "revision_shortfall", false);
  } finally {
    await fs.rm(workspace, { recursive: true, force: true });
  }
});
```

- [ ] **Step 2: 修复引擎判定条件**

`src/core/agent-engine.mjs:609` 从：

```js
  if (Number.isFinite(request.shortfall) && request.shortfall > 0) {
    runtime.modelClient.costTracker?.recordRefill?.();
  }
```

改为：

```js
  if (request.kind === "revision_shortfall") {
    runtime.modelClient.costTracker?.recordRefill?.();
  }
```

- [ ] **Step 3: 跑通**

Run: `node --test tests/cost-tracker.test.mjs tests/agent-engine.test.mjs`
Expected: PASS

- [ ] **Step 4: 回归 + Commit**

Run: `npm test && npm run verify:mvp`
Expected: 全绿（verify:mvp 含字数不足补写路径，确认 revision_shortfall 仍正确计数）

```powershell
git add src/core/agent-engine.mjs tests/cost-tracker.test.mjs tests/agent-engine.test.mjs
git commit -m "fix(refill): only count revision_shortfall as refill, not quality-gate revisions"
```

---

## Task 3: deriveBadges 预警升级 + 章节抽屉行成本

**Files:**
- Modify: `src/app-shell/agent-truth.mjs:119-124`
- Modify: `src/app-shell/drawer-panels.js:53-89`
- Modify: `tests/agent-truth.test.mjs`

两个 Critical 合在一个 Task 里——都是前端显示层，改同批文件。

- [ ] **Step 1: 写 deriveBadges 失败测试**

`tests/agent-truth.test.mjs` 追加（`deriveBadges` 已 import 于文件顶部——若未 import 则加 `import { deriveBadges } from "../src/app-shell/agent-truth.mjs";`）：

```js
test("deriveBadges 在最近事件含 chapter_cost_warning 时成本徽章至少为 warning", () => {
  const dashboard = {
    summary: { estimatedCost: 0.1, targetChapters: 10, completedChapters: 1 },
    project: { budget_config: { max_cost: 100 } },
    events: [{ type: "chapter_cost_warning" }]
  };
  const badges = deriveBadges(dashboard);
  assert.equal(badges.cost.level, "warning");
});

test("deriveBadges 超预算 over 优先级高于预警事件", () => {
  const dashboard = {
    summary: { estimatedCost: 120, targetChapters: 10, completedChapters: 1 },
    project: { budget_config: { max_cost: 100 } },
    events: [{ type: "chapter_cost_warning" }]
  };
  assert.equal(deriveBadges(dashboard).cost.level, "over");
});

test("deriveBadges 无预警无超预算时成本徽章为 normal", () => {
  const dashboard = {
    summary: { estimatedCost: 0.1, targetChapters: 10, completedChapters: 1 },
    project: { budget_config: { max_cost: 100 } },
    events: []
  };
  assert.equal(deriveBadges(dashboard).cost.level, "normal");
});
```

Run: `node --test tests/agent-truth.test.mjs`
Expected: FAIL（当前 deriveBadges 不看 events）

- [ ] **Step 2: 实现 deriveBadges 预警升级**

`src/app-shell/agent-truth.mjs:119-124` 替换为：

```js
  const used = dashboard?.summary?.estimatedCost ?? 0;
  const budget = dashboard?.project?.budget_config?.max_cost ?? 0;
  const pct = budget > 0 ? used / budget : 0;
  const recentCostWarning = (dashboard?.events ?? []).some(
    (event) => event.type === "chapter_cost_warning"
  );
  let level = 'normal';
  if (pct >= 1) level = 'over';
  else if (pct >= 0.8 || recentCostWarning) level = 'warning';
```

（`dashboard.events` 字段名已核实：`app-dashboard.mjs:101` 返回 `events`，每条含 `type`。）

Run: `node --test tests/agent-truth.test.mjs`
Expected: PASS

- [ ] **Step 3: 章节抽屉行成本——改 buildChapterRow 签名与传参**

`src/app-shell/drawer-panels.js:62-65` 循环改为：

```js
      const byChapter = data.cost?.byChapter ?? {};
      const costAvailable = data.summary?.costAvailable === true;
      for (const chapter of chapters) {
        list.append(buildChapterRow(chapter, byChapter[String(chapter.chapter_no)], costAvailable));
      }
```

`buildChapterRow` 签名（:71）改为 `(chapter, costRow, costAvailable)`，完成态 meta（:86-88）改为：

```js
      const meta = document.createElement("span");
      meta.className = "ch-meta mono";
      if (costAvailable && costRow) {
        meta.textContent = `${formatNumber(chapter.actual_words)} 字 · ${formatMoney(costRow.estimatedCost)}`;
      } else {
        meta.textContent = `${formatNumber(chapter.actual_words)} 字`;
      }
```

（`formatMoney` 已在该文件 import 列表里。）

- [ ] **Step 4: 回归 + Commit**

Run: `npm test && npm run verify:app-shell`
Expected: 全绿

```powershell
git add src/app-shell/agent-truth.mjs src/app-shell/drawer-panels.js tests/agent-truth.test.mjs
git commit -m "feat(cost): deriveBadges warning upgrade on chapter_cost_warning + chapter row cost"
```

---

## Task 4: thread-renderer 成本守卫 + 缓存节省行守卫

**Files:**
- Modify: `src/app-shell/thread-renderer.js:324`
- Modify: `src/app-shell/components/cost-panel.js:114-123`
- Modify: `tests/app-shell/cost-panel.test.mjs`

两个显示诚实化收尾——都是"不配价格时不显示 0"原则的遗漏点。

- [ ] **Step 1: 修 thread-renderer**

`src/app-shell/thread-renderer.js:324` 从：

```js
    const cost = data.summary?.estimatedCost ? ` · 约 ${data.summary.estimatedCost}` : "";
```

改为：

```js
    const cost = (data.summary?.costAvailable && data.summary?.estimatedCost) ? ` · 约 ${data.summary.estimatedCost}` : "";
```

- [ ] **Step 2: 修缓存节省行守卫**

`src/app-shell/components/cost-panel.js:114-123` 的 `buildCacheHealth` 从：

```js
function buildCacheHealth(cost) {
  const rates = cost?.recentHitRates ?? [];
  const saved = Number(cost?.cacheSavedCost ?? 0);
  const mean = meanHitRate(rates);
  const meanPct = (mean * 100).toFixed(1);
  return section("缓存健康",
    row(`最近 ${SPARKLINE_LENGTH} 次命中率`, `${meanPct}%`, "mono"),
    row("缓存节省", `${saved.toFixed(2)} 元`, "mono"),
    buildSparkline(rates)
  );
}
```

改为：

```js
function buildCacheHealth(cost) {
  const rates = cost?.recentHitRates ?? [];
  const saved = Number(cost?.cacheSavedCost ?? 0);
  const costAvailable = cost?.costAvailable ?? false;
  const mean = meanHitRate(rates);
  const meanPct = (mean * 100).toFixed(1);
  const children = [
    row(`最近 ${SPARKLINE_LENGTH} 次命中率`, `${meanPct}%`, "mono"),
    buildSparkline(rates)
  ];
  if (costAvailable && saved > 0) {
    children.splice(1, 0, row("缓存节省", `${saved.toFixed(2)} 元`, "mono"));
  }
  return section("缓存健康", ...children);
}
```

- [ ] **Step 3: 更新 cost-panel 测试**

`tests/app-shell/cost-panel.test.mjs` 找到断言"缓存节省 0.00 元"的用例（搜索 `saved.toFixed` 或 `缓存节省`），改为：

当 `costAvailable: false` 时不出现"缓存节省"行，断言 children 中不含 `缓存节省` 文本。

当 `costAvailable: true, cacheSavedCost: 0.6` 时出现 `缓存节省 0.60 元`，断言值正确。

- [ ] **Step 4: 回归 + Commit**

Run: `node --test tests/app-shell/cost-panel.test.mjs && npm test`
Expected: 全绿

```powershell
git add src/app-shell/thread-renderer.js src/app-shell/components/cost-panel.js tests/app-shell/cost-panel.test.mjs
git commit -m "fix(cost-honesty): guard estimatedCost with costAvailable in thread-renderer, hide cache savings when unpriced"
```

---

## Task 5: el() attrs 支持 + a11y 修复

**Files:**
- Modify: `src/app-shell/components/cost-panel.js:21-36`
- Modify: `tests/app-shell/cost-panel.test.mjs`

**根因：** `el()` 帮手不处理 `role`/`aria-label` 等属性，buildSparkline 和 buildWarningBanner 传入的 attrs 被静默丢弃。3c5b8ad 这个专门为 a11y 打的修复提交实际没改变 DOM 输出。

- [ ] **Step 1: 修改 el() 帮手**

`src/app-shell/components/cost-panel.js:21-36` 的 `el()` 函数，在 `props.style` 分支之后加：

```js
  if (props.attrs) {
    for (const [k, v] of Object.entries(props.attrs)) {
      if (v != null) node.setAttribute(k, String(v));
    }
  }
```

然后 `buildSparkline`（:74-97）的 `el("div", { className: "cost-sparkline", role: "img", ... })` 改为把 `role` 和 `aria-label` 移入 `attrs`：

```js
function buildSparkline(recentHitRates) {
  const wrap = el("div", {
    className: "cost-sparkline",
    dataset: { costSpark: "1" },
    attrs: {
      role: "img",
      "aria-label": `最近 20 次缓存命中率，平均 ${(meanHitRate(recentHitRates) * 100).toFixed(1)}%`
    }
  });
```

`buildWarningBanner`（:171-183）同理：

```js
function buildWarningBanner(warning) {
  if (!warning) return null;
  const chapterNo = warning.chapter_no ?? warning.data?.chapter;
  const chapterText = chapterNo != null ? `第 ${chapterNo} 章` : "未知章节";
  return el("div", {
    className: "cost-warning-banner",
    attrs: { role: "status" },
    dataset: { costWarningBanner: "1", chapterNo: String(chapterNo ?? "") }
  },
```

- [ ] **Step 2: 补测试**

`tests/app-shell/cost-panel.test.mjs` 的 `MockElement` 类加：

```js
  setAttribute(name, value) {
    if (!this._attrs) this._attrs = {};
    this._attrs[name] = value;
  }
  getAttribute(name) {
    return this._attrs?.[name] ?? null;
  }
```

找 buildSparkline 的测试用例，加断言：

```js
  assert.equal(sparklineEl.getAttribute("role"), "img");
  assert.ok(sparklineEl.getAttribute("aria-label").includes("命中率"));
```

找 buildWarningBanner 的测试用例，加断言：

```js
  assert.equal(bannerEl.getAttribute("role"), "status");
```

- [ ] **Step 3: 回归 + Commit**

Run: `node --test tests/app-shell/cost-panel.test.mjs`
Expected: PASS

```powershell
git add src/app-shell/components/cost-panel.js tests/app-shell/cost-panel.test.mjs
git commit -m "fix(a11y): el() attrs support so sparkline role and warning banner role actually render"
```

---

## Task 6: clickability 探针补全

**Files:**
- Modify: `scripts/verify-app-clickability.cjs`

**根因：** CLAUDE.md 把"按钮看得到但点不动"列为高频回归面。计划 Task 6/8 明确要求 5 个新设置输入框和成本三段视图探针，实际全缺。

- [ ] **Step 1: 补 5 个新设置输入框探针**

在 `verify-app-clickability.cjs` 的设置弹窗探针区域（`settings-api-key-copy-empty` 之后、`settings-cancel` 之前，约 :222-276 之间），插入：

```js
  // --- 价格与预算上限输入框探针 ---
  const priceInputState = await read(win, `
    (() => {
      const control = document.querySelector('[aria-label="输入价（元/百万 token）"]');
      if (!control) return { tagName: null };
      control.value = "2";
      control.dispatchEvent(new InputEvent("input", { bubbles: true, inputType: "insertText", data: "2" }));
      return { tagName: control.tagName, value: control.value };
    })()
  `);
  assert.equal(priceInputState.tagName, "INPUT", "price input must be editable");
  assert.equal(priceInputState.value, "2", "price input must accept numeric value");

  const priceOutputState = await read(win, `
    (() => {
      const control = document.querySelector('[aria-label="输出价（元/百万 token）"]');
      if (!control) return { tagName: null };
      control.value = "8";
      control.dispatchEvent(new InputEvent("input", { bubbles: true, inputType: "insertText", data: "8" }));
      return { tagName: control.tagName, value: control.value };
    })()
  `);
  assert.equal(priceOutputState.tagName, "INPUT", "price output must be editable");

  const maxCostState = await read(win, `
    (() => {
      const control = document.querySelector('[aria-label="成本上限（元，需先配置价格）"]');
      if (!control) return { tagName: null };
      return { tagName: control.tagName };
    })()
  `);
  assert.equal(maxCostState.tagName, "INPUT", "max cost input must exist");

  const maxTokensState = await read(win, `
    (() => {
      const control = document.querySelector('[aria-label="token 总量上限"]');
      if (!control) return { tagName: null };
      return { tagName: control.tagName };
    })()
  `);
  assert.equal(maxTokensState.tagName, "INPUT", "max tokens input must exist");
```

（`priceCacheHit` 字段可选，非关键路径，不探。4 个必填/必现字段已覆盖。）

- [ ] **Step 2: 补成本页签三段视图断言**

成本页签探针（:283-287）的 `expect` 回调从：

```js
    expect: () => read(win, "document.querySelector('[data-dtab=\"cost\"]').getAttribute('aria-selected') === 'true' && document.querySelector('.cost-panel-root') !== null"),
```

改为：

```js
    expect: () => read(win, `
      document.querySelector('[data-dtab="cost"]').getAttribute('aria-selected') === 'true'
      && document.querySelector('.cost-panel-root') !== null
      && document.querySelectorAll('[data-cost-section]').length >= 2
    `),
```

（三段视图至少有 2 个 `data-cost-section`——总览和缓存健康；章节成本段无章节时可能不渲染。断言 `>= 2` 覆盖有项目场景。）

- [ ] **Step 3: 验证**

Run: `npm run verify:app-clickability`
Expected: `ok: true`（含新输入框和三段断言）

Run: `npm run verify:app-shell && npm test`
Expected: 全绿

- [ ] **Step 4: Commit**

```powershell
git add scripts/verify-app-clickability.cjs
git commit -m "fix(clickability): probe new pricing/budget inputs and cost section structure"
```

---

## Task 7: 交付报告失实修正

**Files:**
- Modify: `docs/superpowers/reports/2026-06-11-s1-delivery-report.md`

- [ ] **Step 1: 修正"预警徽章"表述**

报告第 25 行 `feat(cost-ui)` 的说明从：

```
三段式面板（总览/缓存健康/章节成本）+ sparkline + 预警徽章 + a11y aria-label
```

改为：

```
三段式面板（总览/缓存健康/章节成本）+ sparkline + 面板内预警 banner（主界面成本徽章升级 + 章节行成本在收尾修复中补齐）
```

- [ ] **Step 2: 删除已知问题 #3（不存在的 bug）**

报告"已知问题与 S2 回退建议"表中删除第 3 行：

```
| 3 | `cachedTokens` vs `cacheHitTokens` 计算口径不一致 | `cost-tracker.mjs:54,99` | **真实 bug**：`estimateCost` 用一个字段，`cacheSavedCost` 用另一个。S2 统一 |
```

两处表达式完全相同（`cacheHitTokens ?? cachedTokens` 再 `Math.min(…, input)`），这不是 bug。

- [ ] **Step 3: 在已知问题表新增本轮修复项**

追加：

```
| 10 | stage override pricing 被设置层剥离 | `settings-runtime.mjs:232` | 本轮已修复：normalizeStageOverrides 补 pricing 拷贝 |
| 11 | refill 过计：技能门禁修订也算补写 | `agent-engine.mjs:609` | 本轮已修复：改为 kind === "revision_shortfall" |
| 12 | deriveBadges 不响应 chapter_cost_warning | `agent-truth.mjs:119` | 本轮已修复 |
| 13 | 章节抽屉行缺成本 | `drawer-panels.js:64` | 本轮已修复 |
| 14 | thread-renderer 直显 estimatedCost 不守 costAvailable | `thread-renderer.js:324` | 本轮已修复 |
| 15 | cost-panel el() 丢弃 role/aria-label | `cost-panel.js:21` | 本轮已修复 |
| 16 | 缓存节省 0.00 元在未配价格时显示 | `cost-panel.js:121` | 本轮已修复 |
```

- [ ] **Step 4: Commit**

```powershell
git add docs/superpowers/reports/2026-06-11-s1-delivery-report.md
git commit -m "docs(s1): fix delivery report — remove false bug #3, correct badge claim, add fix items"
```

---

## 验收对照

| 审查问题 | 严重度 | 对应 Task |
|----------|--------|-----------|
| deriveBadges 预警升级缺失 | Critical | Task 3 |
| 章节抽屉行成本缺失 | Critical | Task 3 |
| stage override 价格剥离 | Important | Task 1 |
| refill 过计 | Important | Task 2 |
| thread-renderer 直显 | Important | Task 4 |
| 缓存节省 0.00 元 | Important | Task 4 |
| el() attrs 丢弃 | Important | Task 5 |
| clickability 探针缺失 | Important | Task 6 |
| 交付报告失实 | Minor | Task 7 |

## 风险提示

1. **Task 1 的 pricing 保留要同时测清除（null）和无效价格拒绝**——`normalizePricing` 已经处理了，但 `normalizeStageOverrides` 的循环里需要确认只对有效 stage 名生效。
2. **Task 2 改判定条件后，verify:mvp 的补写场景必须仍通过**——revision_shortfall kind 不会变，verify:mvp 走的是字数不足补写路径，kind 一定是 revision_shortfall。
3. **Task 6 探针里的 aria-label 文案要与 settings-modal.js 的 `settingField` 产出一致**——`settingField` 用第一个参数作 aria-label，已在 Step 1 里照抄。
