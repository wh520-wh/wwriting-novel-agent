# 第十八轮设计规格：文档基线重建

> 文档性质：设计规格（Design Spec）；文档轮，不拆 tickets（任务数 < 5，单会话执行）。
> 状态：需求已对齐（2026-09-05 grilling 收敛：范围与 R1 口径两题用户确认；遗留对账、分级表排期、数字依据、规格形式四题按推荐默认采纳）。
> 日期：2026-09-05
> **变更记录（2026-09-05，用户决策）**：原仓库根 `WWRITING.md` 删除，项目记忆职能并入 `AGENTS.md`；进度与欠账清单迁至 `docs/memory/project-progress.md`。T1 由「WWRITING.md 回填」改为「项目记忆迁移」。约束：产品内每个写作工作区的同名 `WWRITING.md`（`src/core/project-memory.mjs` `PROJECT_MEMORY_FILE`）是产品行为，不在本轮范围。已核实仓库根文件无任何代码/测试/脚本依赖（脚本中的 `WWRITING_*` 均为环境变量名；工作区模板内建于 project-memory.mjs；测试引用均为工作区路径 fixture）。
> 适用范围：`AGENTS.md`、`docs/memory/project-progress.md`、`README.md`、`README.en.md`、删除 `WWRITING.md`、本规格；不触碰 `src/`、`tests/`、`scripts/`。
> 输入证据：2026-09-05 审计会话实测（`wc -l` / `git status` / `git log` / grep，数字在各节注明）；`docs/design/2026-08-28-round17-spec.md`（含执行记录回填）。
> 前置条件：T0 收尾提交完成、工作树干净之后，才启动 T1-T3。

---

## 0. 总览

- **改什么**：① 提交已暂存的 artifacts 出库与治理约束文档（T0，属十七轮扫尾）；② 删除仓库根 `WWRITING.md`，项目记忆并入 AGENTS.md，进度与欠账迁 `docs/memory/project-progress.md`，回填第十七轮（T1）；③ README 双语可变数字补「记录于 + 依据」（T2）；④ R1 红线口径订正并立「样式体积追踪线」（T3）。
- **为什么**：四个互相矛盾的测试数在档（P1）；进度段停在第十六轮、第十七轮内容只在 git log 里（P2）；「全库落线」结论与两个 CSS 文件现状冲突且口径未注明（P3）；过程产物出库停在暂存区（P4）；双规则文件（WWRITING/AGENTS）同步漂移负担（P5，变更记录引入）。
- **影响**：仅文档与 Git 索引变动；零代码、零测试改动；README 对外数字换为有据可查的实跑值；后续轮次（分级表 / styles.css 审计 / 测试清单）以本轮产出的欠账清单与追踪线为输入。

---

## 1. 背景与问题清单（现状证据，记录于 2026-09-05）

- **P1 测试数四值矛盾**：1974（WWRITING 第十六轮，08-24）｜1976（CHANGELOG v0.5.1，08-27）｜1982（round17 规格门禁 1，08-29）｜1986（`README.md:49,155`、`README.en.md:47,136`，无日期无依据）。四个值各自有其历史口径，但「当前值」无一处可复现。
- **P2 进度段停更**：原 `WWRITING.md`「当前进度」止于第十六轮（2026-08-24）。第十七轮已于 08-28 至 08-30 落入主干（带 round17 标记提交 30 个；另有无标记尾部提交 2401ba8、dcf4faf 等），项目记忆缺失。
- **P3 口径冲突**：第十六轮记录宣称「全库扫描 124 文件全部 ≤1200（最大 journal.mjs 1184）」，实测 `styles.css` 2613 行、`agent/agent.css` 2171 行（`wc -l`，2026-09-05）。R1 红线从未注明扫描口径，且无守卫脚本（`scripts/` 无行数检查逻辑），属人工扫描口径。
- **P4 artifacts 出库半途**：391 个文件已从索引移出（暂存删除）且 `.gitignore` 已覆盖 `artifacts/`，**本地文件保留在磁盘**；同时工作树有六条治理约束的文档修改未提交。
- **P5 双规则文件漂移**：工程规则同时存在于 WWRITING.md「工程收敛规则」与 AGENTS.md 摘要，需人工双向同步（AGENTS.md 文档规则末条即为此设）；用户决策合并为单入口。

---

## 2. 范围

### 2.1 本轮包含

T0 收尾提交 → T1 项目记忆迁移 → T2 README 补依据 → T3 R1 口径订正，验收见 §5。

### 2.2 本轮明确不做（对齐决议 2026-09-05）

- **产品内工作区 `WWRITING.md`**：`PROJECT_MEMORY_FILE`、提示词、迁移与相关测试全部不动——它是产品功能，删除仅限仓库根这份开发文档。
- **CHANGELOG 与版本号**：归 ship 流程（round17 规格执行记录同口径）；v0.5.1 之后提交的发布记录留到发 v0.5.2 时统一写。
- **产品功能分级表**：设为第十九轮（产品收敛轨道首交付物）；本轮欠账清单只合并不排序，排序归分级表。
- **styles.css / agent.css 内容审计**：设为第二十轮，判据用第十九轮分级表；本轮只立追踪线，不动样式。
- **测试治理清单**：第十九轮并行产出（只读）；本轮欠账清单中测试覆盖类条目仅登记归属。
- **不新增守卫脚本**：R1 至今是人工扫描口径，本轮只做文档订正，不引入自动化（避免扩 scope）。
- **不改写历史轮次记录的当时值**：`docs/` 历史文档只在必要处**追加订正注**，数字原样保留。
- **零 `src/` / `tests/` / `scripts/` 改动**：本轮不做任何代码与测试变更。

---

## 3. 任务明细

### T0 开工收尾（两个提交；属第十七轮扫尾，不计入本轮目标）

- **T0a** `chore(artifacts): 过程产物移出 Git`——提交暂存区中 391 个 `artifacts/` 删除与 `.gitignore` 的 `artifacts/` 行。本地文件保留不删；提交说明注明对 round17 提交 5dcde30 的规则反转。**已完成：0f9307b。**
- **T0b** `docs: 六条治理约束入库`——提交工作树中 AGENTS.md / WWRITING.md / README 双语的约束修改（README 的 1986 为无依据值，T2 复核）。**已完成：f515966。**

### T1 项目记忆迁移（原「WWRITING.md 回填」，按变更记录改写）

- **T1a AGENTS.md 承接**：新增「工程收敛规则（给后续模型）」节（自 WWRITING.md 逐字迁入，T3 再订正 R1 条目）、「写作工作区约定」节（项目定位/当前有效要求/写作风格/权威文件逐字迁入）；「开工必读」改指本文件规则节与 `docs/memory/project-progress.md`；「文档时间与数字规则」末条改为「本文件是工程规则唯一入口」；顶部注明仓库根 WWRITING.md 删除与产品工作区同名文件的区别。
- **T1b `docs/memory/project-progress.md` 新建**：「当前基线」（1986/1986 实跑、样式 2613/2171、journal 1183/runtime 1150，均带依据）＋「当前进度」（第九至十六轮逐字迁移；第十七轮回填段：双线结构、第一/第二部分要点、尾部修复、配套、验收门禁 1982 pass 与反馈环 10/10，规模口径注明）＋「当前欠账清单」（15 项表格，来源与去向，只记不排）。
- **T1c 删除与引用核查**：`git rm WWRITING.md`；全库 grep 结论——`src/`/`tests/`/`scripts/` 对仓库根文件零依赖（env 变量名/工作区 fixture/内建模板均不受影响）；README 双语的 `WWRITING.md` 引用全部是产品工作区文件说明，不修改；CLAUDE.md 无引用。
- **T1d 本规格随 T1 提交入库。**

### T2 README 中英补依据

- **基线注一行**放在「当前版本」下方（整篇同一基线放顶部，避免逐行打标）：`> 测试数等统计基线：记录于 2026-09-05｜依据：npm test 实跑（1986/1986，exit 0）`（英文版等价表述）。
- **数字复核结论**：`npm test` 实跑 1986 pass / 0 fail（2026-09-05，93s）——README 现值 1986 与实跑一致，**数字不改**，只补依据；P1 的四值矛盾由「当前值有据 + 历史值各留原处」消解。
- **中英一致**：基线注双语等价；版本描述、历史段落不动。

### T3 R1 口径订正 + 样式体积追踪线（AGENTS.md「工程收敛规则」节）

- 「体积红线」条目改写：明示口径为「**逻辑源码（`.js`/`.mjs`）单文件 ≤1200 行**」；追加「**样式体积追踪线**：`src/app-shell/styles.css` 与 `src/app-shell/agent/agent.css` 登记现状值（2613 / 2171，记录于 2026-09-05），超线即适用『先停止加样式，优先删重复』义务；量化目标值由第二十轮审计轮测定后回填，本轮不臆造阈值」。
- **AGENTS.md 同步检查**：T1 合并后本文件即唯一入口，无第二份规则文件需同步。

---

## 4. 执行顺序

T0a → T0b → `npm test` 复跑（数字来源兼健康检查；若红，先停下报告，不带病写文档）→ T1a → T1b → T1c → T1d 提交 → T2 → T3 → §5 自检 → 提交。

---

## 5. 验收标准（可勾选）

1. `git status` 干净（T0 后与收尾各验一次）。
2. 仓库根 `WWRITING.md` 已删除；AGENTS.md 含「工程收敛规则」「写作工作区约定」承接节；`docs/memory/project-progress.md` 含第九至十七轮进度与 15 项欠账清单。
3. 进度文件内全部可变数字带「记录于 + 依据」。
4. 第十六轮记录的口径问题以「当前基线 + R1 订正」消解，历史数字未改写。
5. README 双语基线注在档且等价；四处测试数（README.md:49,155、README.en.md:47,136）等于实跑值 1986。
6. R1 现行文本含「.js/.mjs」口径；样式追踪线含两文件现状值与回填约定。
7. `git diff --stat`（dcf4faf..HEAD）确认零 `src/`、`tests/`、`scripts/` 改动。
8. `npm test` 输出摘要留存于 §7 执行记录。

---

## 6. 遗留下轮

- **第十九轮**：产品功能分级表（主路径/恢复能力/验证能力/增强功能），测试治理清单并行产出（只读，先列清单不删除）。
- **第二十轮**：styles.css / agent.css 重复样式与状态收敛（判据 = 第十九轮分级表；顺带候选：session-sidebar 注释订正）。
- 悬空项（完读率回放、真实 API 复跑等）已全部入 `docs/memory/project-progress.md` 欠账清单，不再单列。

---

## 7. 执行记录（回填）

- T0a：0f9307b（392 文件：391 删除 + .gitignore）。
- T0b：f515966（AGENTS.md +20 / README 双语 1953→1986 / WWRITING.md +11）。
- `npm test`：**1986 pass / 0 fail**（2026-09-05，exit 0，93s）——README 现值与实跑一致。
- （T1/T2/T3 完成后回填提交号与自检结果。）
