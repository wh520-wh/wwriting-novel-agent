# 更新日志

## v0.5.1（2026-08-27）

自 v0.5.0 以来的 42 个提交。全量测试 1976/1976。

这版做两件事：把启动过程收拾到桌面应用该有的样子，以及修一轮系统扫描找出的交互死角。

### 启动体验

- 主窗口提前到服务端组装之前创建，首帧可绘才显示。以前是后端就绪、dashboard 探测通过之后才开窗，用户面对一段什么都没有的等待；现在 Chromium 预热和后端初始化并行跑，页面加载也不再串排在后端探测后面
- 页面在被绘制之前就把上次选择的主题写进 html 标签。夜间用户不会再看到先白一下再变黑
- 左栏项目列表在数据到达前显示骨架占位行（跟随系统偏好时也一并处理），不再是空白
- 启动阶段出错会弹系统错误框并退出。此前是静默抛异常，留下一个没有任何提示的死进程

### 超长会话与压缩

- 投影查询改读日志尾部窗口，10 万事件量级的老会话打开不再整段卡死
- 压缩失败时输入请求被诚实拒绝：进不去就明说，不再排进一个已经没有消费者的队列。手动取消压缩也不会误伤已排队输入
- SSE 流放弃重连前补一次终态上报，流死掉时界面有交代了

### 配置与其他

- 设置页保存密钥后立即生效，第一次配置模型不用再重启应用
- dashboard 打开前先把脏成本写回 cost.json，成本统计不再长期落后于实际用量
- 记忆面板读取失败降级为占位文案，其余卡片照常渲染
- waitForServer 对不可达端口统一退避重试，消除 8 秒热循环
- 竞品调研与前端 bug 扫描报告入库 docs/

## v0.5.0（2026-08-23）

自 v0.4.0（2026-08-06）以来的 369 个提交，跨越八轮迭代（第八轮至第十五轮）。全量测试 1953/1953。

### 会话与运行

- 多会话架构落地：同一个项目内支持多个会话并行，会话数据按项目分片存储，底层按需懒创建并受项目占用门禁保护
- 界面新增两级"项目—会话"侧边栏，支持会话切换、自动命名（首条消息后）、归档会话列表
- 输入生命周期闭环：排队输入可撤回，优先级输入在安全点调度
- 超时语义改为"空闲超时 + 总截止硬闸"，支持 `timeout_ms` / `total_deadline_ms` 配置；`finish_reason=length` 截断状态透传
- 工具自动期限强制：长时间未返回的工具调用被自动关闭，不再拖垮整个 Run；Windows 下取消前清理 shell 进程树

### 上下文与压缩

- Journal 分段存储，支持尾部翻页与历史生命周期管理，中断后从快照恢复游标
- 可见、可取消的上下文压缩：压缩进度可见、压缩门禁收敛、失败后可恢复
- 压缩链路瘦身：大工具输出本地截断摘要、预算优先驱逐、手动 `/compact` 放宽限制
- 上下文仪表与圆环（含悬停显示会话累计缓存命中率）
- 模型请求超窗降级：上下文窗口八成阈值推导、volatile 大工具输出超硬窗口时降级摘要

### 模型与配置

- 模型配置重构为"供应商 / 模型"两级存储（v2），内置 DeepSeek、MiMo 预设目录，旧版 v1 清单自动迁移
- 模型设置页：供应商与模型的增删改、启停、设默认、删除确认，拉取模型展开挑选，连接测试行内显示结果
- 模型引用解析收进最底层配置读取：悬空引用降级为默认并附说明，未配置项不再产生半成形引用
- 窗口口径统一（ADR 0004）：上下文窗口 / 最大输出改读配置字段，`[1m]` 尾标机制退役迁移，唯一解析点 `resolveModelLimits`
- 当前模型显示单源化（ADR 0005）：统一显示「厂商 / 模型名」，模型行高级折叠项提供上下文 / 最大输出预设下拉（缺省 256k / 64k）
- 新增 opencode 兼容端点，支持 `allow_stream_eof` 显式开关
- 模型选择器只显示已启用项，未配置时入口直达设置页

### 记忆与章节

- 记忆三件套纪律：提交 / 入账 / 回滚后自动触发 `update_memory` → `book_summary.md` → `WORKLOG.md` 维护链
- 章节与记忆文件版本快照（append-only，每文件 200 版上限），版本时间线面板可查看与恢复
- 新增 `finalize_revision`（章节账目对账入账）、`rollback_chapter`（回滚）工具，写入带期望 checksum 防陈旧覆盖，账本漂移可检测并提示
- 记忆文件迁移到项目根目录，`update_memory` 收敛为参数校验 + 落盘，去除旧水位 / 摘要 / pending / 指纹机制
- 已提交章节文件允许直接编辑（受 checksum 门禁），GBK 编码 shell 输出在 Windows 下正确解码

### 工作区

- 任意文件夹可作为写作工作区（不再要求特定结构），普通文件夹不创建 `project.yaml`
- 工作区设置移出项目目录独立存储；旧项目元数据（`agent_state.json` 等）首开一次性只读迁移
- `/init` 创建并维护项目根 `WWRITING.md` 项目记忆（不生成固定蓝图），Agent 启动时读取
- 新增字数统计咨询工具与写入保护路径声明

### 技能体系

- 技能全插件化（SKILL.md）：目录式技能管理、四层优先级同名覆盖（全局 / 项目 / 用户 / 内置层级），内置保护名单移除，同名冲突按优先级裁决
- 技能目录类目标签与分层选择规则，网文风格体系重写：三基座（网文节奏重心 + AI 红线下沉）、修饰层新增爽感节奏与对白驱动两类，新增流派包 genre-suspense / genre-detective
- 技能输出风格下拉与 `output_style` 死链后端删除

### 界面

- 视觉基线重构为 codex 卡片风格：工作组渲染为带边框可折叠卡片，composer 卡片化，顶栏与项目行打磨
- 任务计划面板：顶栏 chip + 折叠下拉，三态图标单源、pie 计数、删除线展示
- 思考过程可见：reasoning 与正文分离流式写入，思考项 shimmer 扫光、真实耗时标签（"思考 N 秒"）、完成项整行折叠展开
- 脚注式引用协议 `[^n]` 与来源条渲染（refs 转义防注入），Markdown 表格独立滚动层
- 上下文圆环、窄窗口适配（顶栏保留项目标题、设置浮层置顶、紧凑空态）、语义色 token 与信息密度分级
- 流式正文实心光标、web_search 活动行特化渲染（查询 shimmer + 来源逐项勾选）

### 稳定性与修复（精选）

- 崩溃恢复：进程重启后闭合孤儿 tool call / model turn，前端清扫遗留 running 项，不再永久卡死；`process_restarted` 收敛覆盖 stopping / interrupting
- Run 终态定稿：流式叙述气泡终态化，重试不再静默删正文；恢复类操作门禁统一为非终态集
- 网关重试可见：`provider_retry` 事件驱动「重试 n/m」展示，SSE 服务端错误退避重连（上限 5 次）并区分连接中断
- 根除 Node v25 FileHandle GC 文件级失败（活动段句柄惰性打开）；工具期限定时器 flake 根治
- 单实例防多开（锁未持有时二次启动直接退出）；win32 路径大小写归一防成本累计器分裂
- 修复模型发送前 `event_id` 跨实例重复、同名 ID 模型并存保存、自定义模型官方标签误判
- 修复取消流式生成后回复前缀丢失、图片过大 / 历史图片载荷过高导致请求失败、思考型模型连接探测放宽
- 修复 @ 菜单、composer 草稿 / 失败气泡 / 焦点、侧栏活跃指针透传、抽屉滚轮恢复滚动等交互问题
- 安全：SSE 错误事件消毒、digit-suffixed errno 码处理、引用来源条 HTML 转义

### 架构与工程

- 内核分区重构（第十五轮）：agent runtime 3056 → 1142 行，journal 1982 → 1183 行，tools/index 2123 → 1032 行；`reduceEvent` 48 case 拆 handler 表、工具四域注册拆分、常量与微工具单源化
- 前端控制面重排：dom-kit / form-kit 共享层（toast、focusTrap、confirmLayer 单源），view 四分区拆分，app.js 入口化 `bootApp` + refs 分桶
- gsap 退役换 CSS transition，vendor 目录 5629 行删除（motion-runtime API 逐字保持）
- HTTP 业务下沉域模块，守卫三规则立线：src 文件 ≤1200 行、测试 glob 全目录覆盖、agent 内部 seam 禁令
- 新增 ADR 0001-0006 决策记录；统一 Journal / 自动压缩 / 供应商两级重构等规格文档入库
- 桌面端减少依赖下载体积，本地运行自动打开浏览器

### 测试与验证

- 全量回归从 ~1800 涨到 1953/1953，`sim:user-flow` 双模式（mock / 真实 API）21/21
- 视觉验收体系：capture 脚本主题感知（`--theme dark`）、逐图像素对比基线，round-10 至 round-15 证据归档
- 架构守卫测试（1200 行红线 / glob 覆盖 / seam 禁令）、成本审计脚本
