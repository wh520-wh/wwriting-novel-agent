# 安全策略

## 支持的版本

本项目处于早期阶段，仅在最新发布版本上修复安全问题。

| 版本 | 支持 |
| --- | --- |
| 最新 tag（见 Releases） | 是 |
| 更早版本 | 否 |

## 报告漏洞

**请勿通过公开 issue 报告安全问题。**

优先使用 GitHub 的私有漏洞报告通道：进入仓库 **Security** 标签页 → **Report a vulnerability**。该通道仅维护者可见，便于在修复发布前保持信息封闭。

报告时请尽量包含：

- 受影响版本或 commit
- 复现步骤（最小可行复现最佳）
- 实际影响与你的判断依据
- 如涉及数据外泄，说明泄露的内容类型

如果你希望公开致谢，请一并说明使用的署名方式。

## 安全设计前提

本项目的核心设计目标是**数据不出本机**：

- 正文、设定档案、工作日志、检查点全部以普通文件形式存放在用户选择的本地工作区目录
- 模型 API 密钥存放于本机配置文件，不随项目目录分发
- 章节版本库为 append-only 本地目录

因此以下情况都视为安全问题，欢迎报告：

- 提示注入导致 Agent 在用户指定工作区**之外**写入或删除文件
- 项目目录路径穿越（例如通过章节名、技能名、设定字段逃逸工作区）
- API 密钥被写入日志、被输出到对话、或随项目文件被提交
- 检查点 / 版本库被绕过，导致用户无法回滚
- 恢复流程导致正文或记忆文件数据丢失

以下**不属于**本项目的安全边界，不作为漏洞受理：

- 用户自行在提示词或设定文件中填入敏感信息
- 用户主动把工作区目录指向同步盘或公开仓库
- 第三方模型服务自身的数据处理行为
- 本地具备管理员权限的攻击者读取本机文件

## 响应流程

- 确认收到：通常在 3 个工作日内
- 初步评估与影响范围说明：通常在 7 个工作日内
- 修复与发布：视严重程度协商时间线

修复发布后，会在 Release notes 中说明问题性质与影响版本。
