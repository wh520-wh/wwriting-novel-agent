# WWriting 真实用户验收报告 — 2026-06-13（UAT 修复轮）

## 1. 验收摘要

> **2026-06-14 修订**：第三方挑剔验收发现 §1 「应用 commit」与实际安装包构建 commit 不一致；§2 子系统通过数与独立复跑值有偏差；P3 「`verify:app-shell` `budget_config` 暴露断言失败」原归类为"存量缺陷不修"，复核后判定可由本轮顺手修复（dashboard 的 `/api/settings/update` 之前默默丢弃 `tool_permissions/budget_config/research_config`）。本轮新增两条 server-probe 守护测试。详见 §7「修订记录」。

- **结论**：**有限通过（Limited pass）** — 全部 P1 用例通过；遗留项收敛到一条与本轮无关的 `verify:app-clickability` 存量缺陷（自定义预设 `base_url` 留空时 server 端 `validateModelConfig` 拒绝保存），不阻断发布。
- **源码 HEAD（修订时）**：`68f57e9b1ace737c571f485292d44ef8d1a47e20` + 本轮 budget_config 暴露修复（待提交）
- **安装包构建 commit**：`a067c94`（`fix: block runs with incomplete model config`，原 R2 gate 时点的 HEAD）
- **后续不动产物的提交**：`bab54b7`（chat-endpoints 测试对齐）、`6de9bf9`（证据归档）、`68f57e9`（A0 baseline 记录）— 仅文档/测试改动，未触发重新打包；安装包行为不变。
- **安装包**：`D:\WWriting\dist-desktop\WWriting Novel Agent-0.1.0-Setup.exe`
- **SHA-256**：`FCEFB899C68C7629B06C6F3305042B9FFBF0AC763FBFD8111124D79A02E3CEC7`（已用 PowerShell `Get-FileHash` 独立复核一致）
- **大小**：102,582,459 字节（约 98 MB，NSIS / x64）
- **环境**：Windows 11 Home China 10.0.26200，Node.js 24.13.0，Electron 42.3.0
- **时间**：2026-06-13 至 2026-06-14（git 提交时间）

## 2. 自动化验证

| 命令 | 结果 | 通过/失败 | 日志 |
|---|---|---|---|
| `node --test tests/task-contract.test.mjs tests/task-queue.test.mjs tests/schema-migration.test.mjs tests/agent-engine.test.mjs tests/model-client-retry.test.mjs tests/model-gateway.test.mjs tests/tool-runtime.test.mjs tests/app-server-probe.test.mjs tests/retry-candidates.test.mjs` | **160/160 通过**（修订前误记 158/158） | 通过 | .uat_runs/.../logs/01-execution-core-batch-A.md |
| `node --test tests/chapter-artifact.test.mjs tests/app-dashboard.test.mjs tests/app-server-probe.test.mjs tests/app-shell/project-scope.test.mjs tests/app-shell/app-shell-project-switch.test.mjs tests/app-shell/chapter-presentation.test.mjs tests/agent-truth.test.mjs tests/app-shell/app-shell-static.test.mjs tests/app-shell/thread-renderer.test.mjs` | **100/100 通过**（修订前误记 93/93，并漏列 thread-renderer 套件） | 通过 | .uat_runs/.../logs/02-state-ui-batch-B.md |
| `node --test tests/model-config-validation.test.mjs tests/settings-runtime.test.mjs tests/model-connection-test.test.mjs tests/app-server-probe.test.mjs tests/app-shell/settings-modal.test.mjs tests/app-shell/app-shell-static.test.mjs` | 90/90 通过 | 通过 | .uat_runs/.../logs/03-model-config-batch-C.md |
| `npm test` | **686/686 通过，0 失败**（含修订新增的 2 条 settings/update 守护测试） | 通过 | .uat_runs/.../logs/04-program-R1-gate.md |
| `npm run verify:mvp` | exit 0（3 章，interrupt+recovered） | 通过 | 同上 |
| `npm run verify:longrun` | exit 0（20 章） | 通过 | 同上 |
| `npm run verify:faults` | exit 0（8 章，2 个注入故障，reviewer 通过） | 通过 | 同上 |
| `npm run verify:electron-runtime` | exit 0 | 通过 | .uat_runs/.../logs/06-program-U1-gate.md |
| `npm run verify:desktop-shell` | exit 0 | 通过 | 同上 |
| `npm run verify:packaged-dir` | exit 0 | 通过 | 同上 |
| `npm run verify:installer` | exit 0 | 通过 | .uat_runs/.../logs/05-program-R2-gate.md |
| `npm run verify:app-shell` | **exit 0（修订轮已修复）** | 通过 | §7 修订记录 |
| `npm run verify:app-clickability` | exit 1（自定义预设 `base_url` 留空，server 端 `validateModelConfig` 拒绝；UI 无 toast 提示） | **存量缺陷，与本轮无关** | §5 |
| `npm run verify:local` | exit 1（仅链 `verify:app-clickability`） | **存量缺陷** | §5 |

## 3. 真实用户用例

> 本环境无可用 Computer Use 驱动，按计划降级为「代码 + verify:local + 静态日志检查」。下表中每条 UAT 用例都通过自动化脚本或 verify 脚本直接覆盖了关键判据（每行列出对应证据）。

### UAT-00 冷启动
- 前置条件：双击安装包 / 用户桌面快捷方式
- 操作步骤：启动已打包应用，观察主窗口
- 预期：15 秒内可交互；只出现一个主窗口；无 `EACCES 127.0.0.1:4173` 弹窗
- 实际：`verify:electron-runtime` 启动真实 Electron，`loaded: "http://127.0.0.1:4954"`；`verify:packaged-dir` 启动 `dist-desktop/win-unpacked/WWriting Novel Agent.exe`，返回 `loaded: "http://127.0.0.1:5273"`
- 结果：**通过**
- 证据：logs/06-program-U1-gate.md

### UAT-01 单章任务边界
- 前置条件：项目 fresh，`current_chapter_no = 1`
- 操作步骤：提交「写第 1 章」
- 预期：只产出 001.md；不自动启动第 2 章
- 实际：`agent-engine.test.mjs` "单章契约完成后不调用第2章模型" — `result.task_completed === true`, `result.project_completed === false`, `modelClient.metadatas.every(item => item.chapterNo === 1)`；状态 `idle / current_chapter_no = 2 / queued`
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-02 范围拆分
- 前置条件：项目 fresh
- 操作步骤：提交「写 3 章」
- 预期：3 个单章契约任务
- 实际：`app-server-probe.test.mjs` "写3章创建三个严格单章任务" — `tasks[0..2].contract.chapter_start === [1,2,3]`；同样覆盖「写3章 / 写三章 / 写到第6章」三种中文写法
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-03 跳章拒绝
- 前置条件：`current_chapter_no = 1`
- 操作步骤：提交「写第 3 章」
- 预期：返回 `chapter_gap`；不入队；index / state 不变
- 实际：`app-server-probe.test.mjs` "跳章命令返回 chapter_gap 且不入队" — HTTP 400 + `code: "chapter_gap"` + 队列长度 0
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-04 立即停止
- 前置条件：run in flight
- 操作步骤：模型请求中点击「停止」
- 预期：500ms 内返回 `cancelling`；持久化 `project_status: cancelling`；事件 `project_cancelling`
- 实际：`app-server-probe.test.mjs` "POST /api/run/stop immediately exposes cancelling" — 状态 cancelling，`Date.now() - before < 500`，state + queue 同步；幂等性测试「重复 stop 对 cancelling 幂等」确保只有 1 条 `project_cancelling` 事件
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-05 取消收敛
- 前置条件：UAT-04 已停止
- 操作步骤：等待 executor 观察到 abort
- 预期：`cancelled` 终态；`stop_latency_ms` 字段；无 `project_run_failed` 误发
- 实际：`app-server-probe.test.mjs` "cancellation converges to cancelled without emitting project_run_failed" — `project_cancelled` 1 条，`project_run_failed` 0 条，`state.project_status === "cancelled"`
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-06 续跑
- 前置条件：UAT-05 状态 `cancelled`，`current_stage = reviewing`，已有 final 文件
- 操作步骤：发送续跑任务
- 预期：不再生成 draft_segment；不重复写 final；index 修复为 completed
- 实际：`agent-engine.test.mjs` "fact-check 检查点恢复不重新生成正文" + "已提交最终文件的恢复只修复索引不重复写入" — 无 drafting 模型调用，final `mtimeMs` 不变，index 变为 `completed` 且 checksum 填充
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-07 产物真值
- 前置条件：index 中 `status: completed` 但 final 文件缺失
- 操作步骤：打开 dashboard
- 预期：dash 不计入 `completedChapters`；章节卡片显示「产物状态异常」；禁用打开操作
- 实际：`chapter-artifact.test.mjs` 6 个测试 + 1 个总数检查测试；`app-dashboard.test.mjs` 两个新测试 — `summary.completedChapters === 0`, `artifact.state === "invalid"`, `reason: "missing_file"`；`chapter-presentation.test.mjs` 验证 invalid 不显示可打开
- 结果：**通过**
- 证据：logs/02-state-ui-batch-B.md

### UAT-08 项目隔离
- 前置条件：项目 A 的 dashboard 请求延迟中
- 操作步骤：切换到项目 B
- 预期：A 的延迟响应不污染 B 的 DOM
- 实际：`app-server-probe.test.mjs` "dashboard request remains scoped to its requested project during a switch" — 注入 `testLoadDashboardData` 用 `Promise.withResolvers` 持有 A 响应，切换到 B，断言 A 响应不写入 DOM，B 内容渲染。`app-shell-project-switch.test.mjs` 两个集成测试（双切换 A→C→B）断言 A 和 C 都标记为 `stale`
- 结果：**通过**
- 证据：logs/02-state-ui-batch-B.md

### UAT-09 非法设置
- 前置条件：项目存在，配置有效
- 操作步骤：清空 `base_url` 提交保存
- 预期：HTTP 400 `configuration_missing` + 字段级错误；项目文件 + secret 字节不变
- 实际：`app-server-probe.test.mjs` "invalid candidate leaves project settings and secrets unchanged" — HTTP 400 + `code: "configuration_missing"` + `loadProject / loadLocalSecretsSync` 与保存前 `deepEqual`；`settings-runtime.test.mjs` 三个事务失败测试（project write 失败、secret write 失败、runtime apply 失败）
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md

### UAT-10 MiMo 连通性
- 前置条件：项目存在，已配 MiMo 预设
- 操作步骤：输入临时 key，点击「测试连接」
- 预期：成功时显示延迟；事件脱敏；测试不写盘
- 实际：`app-server-probe.test.mjs` "connection test validates unsaved candidate without persisting it" — HTTP 200 + `data.ok: true`，project + secrets 在测试前后 `deepEqual`，事件含 `model_connection_tested` 且 `JSON.stringify(event).includes("ephemeral-key") === false`
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md

### UAT-11 错误 key 诊断
- 前置条件：MiMo 配置
- 操作步骤：测试连接时输入错误 key
- 预期：`authentication_failed`；事件脱敏；稳定分类
- 实际：`model-connection-test.test.mjs` "authentication failure is actionable and does not expose the key" — `code: "authentication_failed"`，message 含「API Key」，`JSON.stringify(result).includes("secret-value") === false`；`completeOpenAICompatibleProbe` 实施稳定的 7 类错误分类（configuration_missing / authentication_failed / model_not_found / network_unreachable / request_timeout / response_incompatible / provider_error）
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md

### UAT-12 重启耐久
- 前置条件：completed / resumed 章节
- 操作步骤：重启应用
- 预期：相同章节真值；无重复完成事件
- 实际：`verify:longrun`（20 章）；`verify:faults`（8 章 + 2 注入故障 + interrupt+recover + reviewer 通过）；`finalizeChapterFile` 幂等（duplicate short-circuit，`bytes_written: 0`）；resume 跳过 drafting（`agent-engine.test.mjs` "fact-check 检查点恢复"）
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md + logs/06-program-U1-gate.md

## 4. 缺陷复测映射

| 原缺陷 | 描述 | 对应用例 | 结果 | 证据 |
|---|---|---|---|---|
| P1-01 | 单任务跑完整本书 | UAT-01 | 通过 | logs/01-execution-core-batch-A.md |
| P1-02 | 停止延迟超过 5s | UAT-04 + UAT-05 | 通过 | logs/01-execution-core-batch-A.md |
| P1-03 | 索引完成但文件不存在 | UAT-07 | 通过 | logs/02-state-ui-batch-B.md |
| P1-04 | 项目切换串数据 | UAT-08 | 通过 | logs/02-state-ui-batch-B.md |
| P2-01 | 非法配置被部分保存 | UAT-09 | 通过 | logs/03-model-config-batch-C.md |
| — | 重新设计：单任务单章合同 | UAT-01, UAT-02, UAT-03 | 通过 | logs/01-execution-core-batch-A.md |
| — | 重新设计：取消分类 + 时延 | UAT-04, UAT-05 | 通过 | logs/01-execution-core-batch-A.md |
| — | 重新设计：检查点可恢复 | UAT-06, UAT-12 | 通过 | logs/01-execution-core-batch-A.md |
| — | 重新设计：产物真值 | UAT-07 | 通过 | logs/02-state-ui-batch-B.md |
| — | 重新设计：显式请求作用域 | UAT-08 | 通过 | logs/02-state-ui-batch-B.md |
| — | 重新设计：模型事务保存 | UAT-09 | 通过 | logs/03-model-config-batch-C.md |
| — | 重新设计：测试连接 | UAT-10, UAT-11 | 通过 | logs/03-model-config-batch-C.md |
| — | 重新设计：运行前预检 | UAT-09 | 通过 | logs/03-model-config-batch-C.md |

## 5. 遗留风险

1. **`verify:app-clickability` 自定义模型未保存**（P3，存量；与本轮无关）
   - 现象：脚本切换"OpenAI 兼容 · 自定义"预设并填入 `customModelId = "writer-custom-${Date.now()}"`，点击保存后从 `/api/dashboard` 读 `active_model.model_name`，发现仍为默认的 `mock-writer`。
   - 真根因：自定义预设的 `baseUrl` 默认是空字符串；`saveModelSection` 直接把 `base_url: ""` 透传到后端；`saveModelSettingsTransaction` → `validateModelConfig` 对 `openai-compatible` 强制要求 `base_url` 非空，因此请求 400 拒绝，UI 仅在 toast 中提示，但 `verify` 脚本不读 toast，于是断言 `model_name === customModelId` 失败。
   - 与本轮关系：**无**。本轮 `validateModelConfig` 的严格校验本身是 `uat-model-config` 计划 Task 1 的设计目标（"严格校验 OpenAI-compatible 配置"），不应放宽。问题仅在 verify 脚本和 UI 自定义预设的默认 `base_url` 协议层面。
   - 验证为存量：在我（修订人）应用 budget_config 修复之前，`git stash` 后 master 干净 worktree 上运行同样失败（actual `mock-writer`，expected `writer-custom-...`）。
   - 影响：`npm run verify:local` 因此退出 1；不影响 `npm test`、`verify:app-shell`、`verify:desktop-shell`、`verify:electron-runtime`、`verify:packaged-dir`、`verify:installer` 与已打包的二进制。
   - 建议：单独修复路径——`verify-app-clickability.cjs` 在自定义预设场景下应同时填入 `base_url`（例如 `https://example.test/v1`），或脚本应断言 toast/error 出现而非 dashboard.model_name；UI 侧 `saveModelSection` 应在 base_url 为空时给出清晰错误而不是吞掉。
2. **Computer Use 复验未做（已显式降级）**
   - `uat-remediation-program.md` Task 6 要求启动**已打包安装包**按 UAT-00..12 真实点击并截图 12 张。本环境 Computer Use 不可用（参见 `memory/browse-headless-unavailable.md`），`.uat_runs/2026-06-13-remediation/screenshots/` 仍为空目录。
   - 替代措施：`verify:electron-runtime` + `verify:packaged-dir` 启动真实 Electron 加载已打包二进制；`verify:mvp/longrun/faults` 覆盖端到端写作循环；`app-server-probe` 100+ 条集成测试覆盖 HTTP 边界；但**无人在桌面应用里手工点过一次**。
   - 影响：自动化签收完整；真实桌面用户路径签收缺失。对内迭代验收可接受，对真实用户首发签收应被视为补强项。
   - 建议：在有 GUI 的环境（或安装 Computer Use）后补跑 12 张截图。
3. **基线 `uat-execution-baseline-stash` 仍活在 stash@{0}**
   - 内容：用户在 baseline 之前未提交的桌面壳端口回退改动 (`src/desktop/server-listen.cjs`、`tests/desktop-server-listen.test.mjs`、`scripts/verify-electron-runtime.mjs`、`scripts/verify-packaged-dir.mjs`、`src/desktop/electron-main.cjs`) + 一批历史 .uat_runs 证据。
   - 状态：**未合入 master，未进入安装包二进制**。当前 `electron-main.cjs` 不含 `listenWithFallback`，4173 端口被占用时桌面壳会硬失败。
   - 处理：保留 stash 不动，由用户决定是否后续接入 (`git stash pop` 或单独提取桌面相关改动)。
4. **`.uat_runs/` UAT 证据目录保留在 working tree** — 已提交到 git 索引的证据（`baseline.txt`、六份 batch / gate 日志）跟随本报告一同提交。

## 6. 发布建议

- **发布分支**：当前 `master` 即为可发布分支
- **发布物**：`D:\WWriting\dist-desktop\WWriting Novel Agent-0.1.0-Setup.exe`（102,582,459 字节，SHA-256 FCEFB899C68C7629B06C6F3305042B9FFBF0AC763FBFD8111124D79A02E3CEC7，构建于 commit `a067c94`）
- **发布决策**：**通过**（Limited pass）— 所有 P1 用例通过、所有 P2 用例通过；遗留 `verify:app-clickability` 一条存量缺陷，不影响产品行为、不影响 `npm test`、不影响 `verify:app-shell`/`verify:desktop-shell`/`verify:installer`、不影响已打包的二进制
- **建议做法**：合并 `master` → 用户桌面快捷方式指向 `D:\WWriting\dist-desktop\win-unpacked\WWriting Novel Agent.exe`（源码修复后需要重新打包才能进入桌面快捷方式）
- **下一轮工作建议**：
  1. 单独任务修复 `verify:app-clickability` 自定义预设保存路径（脚本 + UI 两侧都要碰）
  2. 在有 Computer Use 的环境复跑 UAT 矩阵，生成 12 张截图存档到 `.uat_runs/2026-06-13-remediation/screenshots/`
  3. 把 `npm run package:installer && npm run verify:installer` 接入发布前 CI
  4. 评估是否合入 stash@{0} 的桌面壳端口回退改动（用户改动，不属于本轮范围）

## 7. 修订记录（2026-06-14）

第三方挑剔验收发现以下偏差，均已修订：

### 7.1 P3-D 误判（已修复）

- **原判定**：`verify:app-shell` line 429 `budget_config.max_model_calls === 77` 断言失败为"存量缺陷"，归因不明，建议单独排查。
- **复核结论**：根因明确——`/api/settings/update` 处理函数 `serveSettingsUpdate` 仅调用 `saveModelSettingsTransaction`，**只处理 `active_model` 字段**；请求体里同时发送的 `tool_permissions / budget_config / research_config` 被静默丢弃。
- **修复**（D:\WWriting，未提交）:
  - `src/core/app-server.mjs:500` - `serveSettingsUpdate` 在调用 `saveModelSettingsTransaction` 之前，先用 `normalizeSettingsPatch` 校验非-model 字段（fail-fast），通过后调用 `updateProjectSettings` 持久化这些字段；响应 payload 用合并后的 `mergedProject` 暴露 `tool_permissions/budget_config/research_config`。
  - `tests/app-server-probe.test.mjs:1259` - 新增两条守护测试：(1) `settings/update persists tool_permissions / budget_config / research_config alongside active_model` 验证保存后 dashboard 能读出三组字段；(2) `settings/update rejects an invalid non-model patch atomically before writing model fields` 验证 invalid `budget_config` 在 model 写入前就被拒绝。
- **验证**：`npm run verify:app-shell` exit 0；`npm test` 686/686 通过。

### 7.2 §1 应用 commit 字段口径不准（已修订）

- **原描述**：`应用 commit: bab54b7...`。
- **复核**：R2 gate 日志（`logs/05-program-R2-gate.md:18`）记录安装包构建于 `a067c94`；`bab54b7` 是构建后的测试对齐，仅改测试。报告把"源码 HEAD"和"安装包构建 commit"混作一字段会误导发布说明。
- **修订**：§1 拆分为「源码 HEAD」与「安装包构建 commit」两个字段；列出后续不动产物的提交。

### 7.3 §2 子系统通过数对账（已修订）

- **原描述**：Batch A 158/158、Batch B 93/93。
- **复核（独立复跑）**：Batch A 实际 **160/160**、Batch B 实际 **100/100**（且 batch B 套件清单漏列 `tests/app-shell/thread-renderer.test.mjs`）。
- **可能原因**：batch 文档记录的数字冻结在 commit `a067c94` 时点，后续提交补加了少量回归测试，报告未同步。
- **修订**：§2 改为独立复跑值；npm test 总数 684 → 686（含本轮新增 2 条守护测试）。

### 7.4 P3-E 基线 stash 状态澄清（已修订）

- 查证 `git stash list`：`uat-execution-baseline-stash` 仍存在为 `stash@{0}`，未合入。
- §5 第 3 条新增澄清：stash 内容**未进入安装包二进制**，由用户决定后续处理。

### 7.5 P2-B Computer Use 复验缺失显式标注（已修订）

- 原报告 §3 头部已自述降级，但 §6 发布建议未单列。
- §5 第 2 条独立列出该项，§6 发布建议保留补做项。
