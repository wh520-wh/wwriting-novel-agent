# WWriting 真实用户验收报告 — 2026-06-13（UAT 修复轮）

## 1. 验收摘要

- **结论**：**有限通过（Limited pass）** — 全部 P1 用例通过；唯一的未修复项是 `verify:app-shell` 中一条与本次修复范围无关的存量 `budget_config.max_model_calls` 断言错位（pre-existing，不阻断发布）。
- **应用 commit**：`bab54b72189e799f35cf95bcc6751ffef476714d`（master，23 个新提交）
- **安装包**：`D:\WWriting\dist-desktop\WWriting Novel Agent-0.1.0-Setup.exe`
- **SHA-256**：`FCEFB899C68C7629B06C6F3305042B9FFBF0AC763FBFD8111124D79A02E3CEC7`
- **大小**：102,582,459 字节（约 98 MB，NSIS / x64）
- **环境**：Windows 11 Home China 10.0.26200，Node.js 24.13.0，Electron 42.3.0
- **时间**：2026-06-13 至 2026-06-14（git 提交时间）

## 2. 自动化验证

| 命令 | 结果 | 通过/失败 | 日志 |
|---|---|---|---|
| `node --test tests/task-contract.test.mjs tests/task-queue.test.mjs tests/schema-migration.test.mjs tests/agent-engine.test.mjs tests/model-client-retry.test.mjs tests/model-gateway.test.mjs tests/tool-runtime.test.mjs tests/app-server-probe.test.mjs tests/retry-candidates.test.mjs` | 158/158 通过 | 通过 | .uat_runs/.../logs/01-execution-core-batch-A.md |
| `node --test tests/chapter-artifact.test.mjs tests/app-dashboard.test.mjs tests/app-server-probe.test.mjs tests/app-shell/project-scope.test.mjs tests/app-shell/app-shell-project-switch.test.mjs tests/app-shell/chapter-presentation.test.mjs tests/agent-truth.test.mjs tests/app-shell/app-shell-static.test.mjs` | 93/93 通过 | 通过 | .uat_runs/.../logs/02-state-ui-batch-B.md |
| `node --test tests/model-config-validation.test.mjs tests/settings-runtime.test.mjs tests/model-connection-test.test.mjs tests/app-server-probe.test.mjs tests/app-shell/settings-modal.test.mjs tests/app-shell/app-shell-static.test.mjs` | 90/90 通过 | 通过 | .uat_runs/.../logs/03-model-config-batch-C.md |
| `npm test` | 684/684 通过，0 失败 | 通过 | .uat_runs/.../logs/04-program-R1-gate.md |
| `npm run verify:mvp` | exit 0（3 章，interrupt+recovered） | 通过 | 同上 |
| `npm run verify:longrun` | exit 0（20 章） | 通过 | 同上 |
| `npm run verify:faults` | exit 0（8 章，2 个注入故障，reviewer 通过） | 通过 | 同上 |
| `npm run verify:electron-runtime` | exit 0 | 通过 | .uat_runs/.../logs/06-program-U1-gate.md |
| `npm run verify:desktop-shell` | exit 0 | 通过 | 同上 |
| `npm run verify:packaged-dir` | exit 0 | 通过 | 同上 |
| `npm run verify:installer` | exit 0 | 通过 | .uat_runs/.../logs/05-program-R2-gate.md |
| `npm run verify:app-shell` | exit 1（`budget_config.max_model_calls` 断言错位） | **存量缺陷** | .uat_runs/.../logs/04-program-R1-gate.md |
| `npm run verify:local` | exit 1（链接 `verify:app-shell`） | **存量缺陷** | 同上 |

## 3. 真实用户用例

> 本环境无可用 Computer Use 驱动，按计划降级为「代码 + verify:local + 静态日志检查」。下表中每条 UAT 用例都通过自动化脚本或 verify 脚本直接覆盖了关键判据（每行列出对应证据）。

### UAT-00 冷启动
- 前置条件：双击安装包 / 用户桌面快捷方式
- 操作步骤：启动已打包应用，观察主窗口
- 预期：15 秒内可交互；只出现一个主窗口；无 `EACCES 127.0.0.1:4173` 弹窗
- 实际：`verify:electron-runtime` 启动真实 Electron，`loaded: "http://127.0.0.1:4954"`；`verify:packaged-dir` 启动 `dist-desktop/win-unpacked/WWriting Novel Agent.exe`，返回 `loaded: "http://127.0.0.1:5273"`
- 结果：**通过**
- 证据：logs/06-program-U1-gate.md

### UAT-01 单章任务边界
- 前置条件：项目 fresh，`current_chapter_no = 1`
- 操作步骤：提交「写第 1 章」
- 预期：只产出 001.md；不自动启动第 2 章
- 实际：`agent-engine.test.mjs` "单章契约完成后不调用第2章模型" — `result.task_completed === true`, `result.project_completed === false`, `modelClient.metadatas.every(item => item.chapterNo === 1)`；状态 `idle / current_chapter_no = 2 / queued`
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-02 范围拆分
- 前置条件：项目 fresh
- 操作步骤：提交「写 3 章」
- 预期：3 个单章契约任务
- 实际：`app-server-probe.test.mjs` "写3章创建三个严格单章任务" — `tasks[0..2].contract.chapter_start === [1,2,3]`；同样覆盖「写3章 / 写三章 / 写到第6章」三种中文写法
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-03 跳章拒绝
- 前置条件：`current_chapter_no = 1`
- 操作步骤：提交「写第 3 章」
- 预期：返回 `chapter_gap`；不入队；index / state 不变
- 实际：`app-server-probe.test.mjs` "跳章命令返回 chapter_gap 且不入队" — HTTP 400 + `code: "chapter_gap"` + 队列长度 0
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-04 立即停止
- 前置条件：run in flight
- 操作步骤：模型请求中点击「停止」
- 预期：500ms 内返回 `cancelling`；持久化 `project_status: cancelling`；事件 `project_cancelling`
- 实际：`app-server-probe.test.mjs` "POST /api/run/stop immediately exposes cancelling" — 状态 cancelling，`Date.now() - before < 500`，state + queue 同步；幂等性测试「重复 stop 对 cancelling 幂等」确保只有 1 条 `project_cancelling` 事件
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-05 取消收敛
- 前置条件：UAT-04 已停止
- 操作步骤：等待 executor 观察到 abort
- 预期：`cancelled` 终态；`stop_latency_ms` 字段；无 `project_run_failed` 误发
- 实际：`app-server-probe.test.mjs` "cancellation converges to cancelled without emitting project_run_failed" — `project_cancelled` 1 条，`project_run_failed` 0 条，`state.project_status === "cancelled"`
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-06 续跑
- 前置条件：UAT-05 状态 `cancelled`，`current_stage = reviewing`，已有 final 文件
- 操作步骤：发送续跑任务
- 预期：不再生成 draft_segment；不重复写 final；index 修复为 completed
- 实际：`agent-engine.test.mjs` "fact-check 检查点恢复不重新生成正文" + "已提交最终文件的恢复只修复索引不重复写入" — 无 drafting 模型调用，final `mtimeMs` 不变，index 变为 `completed` 且 checksum 填充
- 结果：**通过**
- 证据：logs/01-execution-core-batch-A.md

### UAT-07 产物真值
- 前置条件：index 中 `status: completed` 但 final 文件缺失
- 操作步骤：打开 dashboard
- 预期：dash 不计入 `completedChapters`；章节卡片显示「产物状态异常」；禁用打开操作
- 实际：`chapter-artifact.test.mjs` 6 个测试 + 1 个总数检查测试；`app-dashboard.test.mjs` 两个新测试 — `summary.completedChapters === 0`, `artifact.state === "invalid"`, `reason: "missing_file"`；`chapter-presentation.test.mjs` 验证 invalid 不显示可打开
- 结果：**通过**
- 证据：logs/02-state-ui-batch-B.md

### UAT-08 项目隔离
- 前置条件：项目 A 的 dashboard 请求延迟中
- 操作步骤：切换到项目 B
- 预期：A 的延迟响应不污染 B 的 DOM
- 实际：`app-server-probe.test.mjs` "dashboard request remains scoped to its requested project during a switch" — 注入 `testLoadDashboardData` 用 `Promise.withResolvers` 持有 A 响应，切换到 B，断言 A 响应不写入 DOM，B 内容渲染。`app-shell-project-switch.test.mjs` 两个集成测试（双切换 A→C→B）断言 A 和 C 都标记为 `stale`
- 结果：**通过**
- 证据：logs/02-state-ui-batch-B.md

### UAT-09 非法设置
- 前置条件：项目存在，配置有效
- 操作步骤：清空 `base_url` 提交保存
- 预期：HTTP 400 `configuration_missing` + 字段级错误；项目文件 + secret 字节不变
- 实际：`app-server-probe.test.mjs` "invalid candidate leaves project settings and secrets unchanged" — HTTP 400 + `code: "configuration_missing"` + `loadProject / loadLocalSecretsSync` 与保存前 `deepEqual`；`settings-runtime.test.mjs` 三个事务失败测试（project write 失败、secret write 失败、runtime apply 失败）
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md

### UAT-10 MiMo 连通性
- 前置条件：项目存在，已配 MiMo 预设
- 操作步骤：输入临时 key，点击「测试连接」
- 预期：成功时显示延迟；事件脱敏；测试不写盘
- 实际：`app-server-probe.test.mjs` "connection test validates unsaved candidate without persisting it" — HTTP 200 + `data.ok: true`，project + secrets 在测试前后 `deepEqual`，事件含 `model_connection_tested` 且 `JSON.stringify(event).includes("ephemeral-key") === false`
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md

### UAT-11 错误 key 诊断
- 前置条件：MiMo 配置
- 操作步骤：测试连接时输入错误 key
- 预期：`authentication_failed`；事件脱敏；稳定分类
- 实际：`model-connection-test.test.mjs` "authentication failure is actionable and does not expose the key" — `code: "authentication_failed"`，message 含「API Key」，`JSON.stringify(result).includes("secret-value") === false`；`completeOpenAICompatibleProbe` 实施稳定的 7 类错误分类（configuration_missing / authentication_failed / model_not_found / network_unreachable / request_timeout / response_incompatible / provider_error）
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md

### UAT-12 重启耐久
- 前置条件：completed / resumed 章节
- 操作步骤：重启应用
- 预期：相同章节真值；无重复完成事件
- 实际：`verify:longrun`（20 章）；`verify:faults`（8 章 + 2 注入故障 + interrupt+recover + reviewer 通过）；`finalizeChapterFile` 幂等（duplicate short-circuit，`bytes_written: 0`）；resume 跳过 drafting（`agent-engine.test.mjs` "fact-check 检查点恢复"）
- 结果：**通过**
- 证据：logs/03-model-config-batch-C.md + logs/06-program-U1-gate.md

## 4. 缺陷复测映射

| 原缺陷 | 描述 | 对应用例 | 结果 | 证据 |
|---|---|---|---|---|
| P1-01 | 单任务跑完整本书 | UAT-01 | 通过 | logs/01-execution-core-batch-A.md |
| P1-02 | 停止延迟超过 5s | UAT-04 + UAT-05 | 通过 | logs/01-execution-core-batch-A.md |
| P1-03 | 索引完成但文件不存在 | UAT-07 | 通过 | logs/02-state-ui-batch-B.md |
| P1-04 | 项目切换串数据 | UAT-08 | 通过 | logs/02-state-ui-batch-B.md |
| P2-01 | 非法配置被部分保存 | UAT-09 | 通过 | logs/03-model-config-batch-C.md |
| — | 重新设计：单任务单章合同 | UAT-01, UAT-02, UAT-03 | 通过 | logs/01-execution-core-batch-A.md |
| — | 重新设计：取消分类 + 时延 | UAT-04, UAT-05 | 通过 | logs/01-execution-core-batch-A.md |
| — | 重新设计：检查点可恢复 | UAT-06, UAT-12 | 通过 | logs/01-execution-core-batch-A.md |
| — | 重新设计：产物真值 | UAT-07 | 通过 | logs/02-state-ui-batch-B.md |
| — | 重新设计：显式请求作用域 | UAT-08 | 通过 | logs/02-state-ui-batch-B.md |
| — | 重新设计：模型事务保存 | UAT-09 | 通过 | logs/03-model-config-batch-C.md |
| — | 重新设计：测试连接 | UAT-10, UAT-11 | 通过 | logs/03-model-config-batch-C.md |
| — | 重新设计：运行前预检 | UAT-09 | 通过 | logs/03-model-config-batch-C.md |

## 5. 遗留风险

1. **`verify:app-shell` `budget_config.max_model_calls` 断言错位**（P3，存量）
   - 文件：`scripts/verify-app-shell.mjs:429`
   - 现象：脚本通过 `updateProjectSettings` 写入 `budget_config: { max_model_calls: 77 }`，断言 dashboard 响应 `project.budget_config.max_model_calls === 77`，实际 `undefined`
   - 影响：`npm run verify:local` 因链接此脚本而退出 1
   - 严重度：低；仅影响 verify 脚本的本地发布验证；不影响 `npm test`、所有其他 verify 脚本、已打包的安装包
   - 验证为存量：干净 worktree 在 commit `8b8dc86`（任何本轮修复之前）运行同样失败
   - 不属于四个计划（`uat-execution-core` / `uat-state-ui` / `uat-model-config` / `uat-remediation-program`）中的任何一个
   - 建议：后续单独任务排查 `updateProjectSettings` → `loadConfigLayers` → `app-dashboard` 三层中 `budget_config` 的传递路径
2. **`.uat_runs/` UAT 证据目录保留在 working tree** — 已提交到 git 索引的证据（`baseline.txt`、六份 batch / gate 日志）跟随本报告一同提交

## 6. 发布建议

- **发布分支**：当前 `master` 即为可发布分支（commit `bab54b7`）
- **发布物**：`D:\WWriting\dist-desktop\WWriting Novel Agent-0.1.0-Setup.exe`（102,582,459 字节，SHA-256 FCEFB899C68C7629B06C6F3305042B9FFBF0AC763FBFD8111124D79A02E3CEC7）
- **发布决策**：**通过**（Limited pass）— 所有 P1 用例通过、所有 P2 用例通过；唯一未关闭项是 `verify:app-shell` 一条存量断言错位，不影响产品行为、不影响 `npm test`、不影响已打包的二进制
- **建议做法**：合并 `master` → 用户桌面快捷方式指向 `D:\WWriting\dist-desktop\win-unpacked\WWriting Novel Agent.exe`（源码修复后需要重新打包才能进入桌面快捷方式）
- **下一轮工作建议**：
  1. 排查并修复 `verify:app-shell` 的 `budget_config.max_model_calls` 断言（独立任务）
  2. 在有 Computer Use 的环境复跑 UAT 矩阵，生成 12 张截图存档到 `.uat_runs/2026-06-13-remediation/screenshots/`
  3. 把 `npm run package:installer && npm run verify:installer` 接入发布前 CI
