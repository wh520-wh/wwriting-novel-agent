# 死代码清理与代码优化计划

基于三份只读扫描报告（已交叉验证）+ 用户三项决策，删除确认的死代码并同步裁剪测试。

## 改动清单

### 1. 核心层 src/core（删除死导出）
- `project-store.mjs`：删除 `readLastCheckpoint`（全仓零引用）
- `chapter-artifact.mjs`：删除 `clearArtifactCache`
- `failures-store.mjs`：删除 `FAILURES_FILE`
- `memory-extractor.mjs`：删除 `MEMORY_EXTRACT_STAGE`（stage 字符串在 agent-engine 硬编码）
- `output-style-loader.mjs`：删除 `listBuiltInStyleNames`
- `skill-runtime.mjs`：删除 `DEFAULT_SKILL_PACK` + `registerProjectSkill` + `activateConditionalSkillsForPaths` + `listActivatedSkills` + 随之无消费者的 `conditionalSkills` 状态与 `_resetConditionalSkills`（用户确认）
- `model-pricing.mjs`：删除 `isDeepSeekModel`（fillOfficialPricing 已不调用）
- `app-dashboard.mjs`：删除 `loadProjectList`（app.js 用的是本地同名函数，不受影响）
- `local-secrets.mjs`：删除 `saveLocalSecret`（saveLocalSecrets 全量保存不调它）
- `provider-adapters.mjs`：删除 `isReasonerModel` 导出；内部 `requiresAutoToolChoice` 改为直接调 `resolveModelCapabilities().supportsThinking`（用户确认；上一计划的"保留"约束已被本次用户决定取代）
- **`writing-intent.mjs` 整文件删除**（用户确认；唯一生产孤立模块）

### 2. 核心层未使用 import
- `agent-engine.mjs:26`：`loadPendingAction`/`savePendingAction`（chat-agent 自己 import）
- `agent-engine.mjs:29`：`previewEditChapter`
- `research-tools.mjs:4`：`writeFileAtomic`
- `side-question.mjs:17`：`readJson`

### 3. 测试裁剪（与 1 配套）
- `tests/provider-adapters.test.mjs`：删 isReasonerModel 用例（:219-230）+ import 裁剪
- `tests/model-pricing.test.mjs`：删 isDeepSeekModel 用例（:100-103）+ import 裁剪
- `tests/skill-runtime-paths.test.mjs`：删 3 个 activateConditionalSkillsForPaths 用例（:37-73），保留 6 个 parseSkillPaths 用例
- `tests/app-dashboard.test.mjs`：删 loadProjectList 断言行（:64-65）+ import 裁剪
- `tests/local-secrets.test.mjs`：改写为直接写 secrets.json（保留 load/apply 断言）
- `tests/app-server-probe.test.mjs:10/:1214`：import 裁剪，setup 改为直接写文件
- `tests/writing-intent.test.mjs`：整文件删除
- 其余测试（event-bus/tool-hooks/permission-defaults 等的 `_`/`__` 测试钩子）**保留**——它们是测试支撑 API，设计如此

### 4. app-shell 层
- `motion-runtime.js`：删除 `animateOrSet`（未进 motion 聚合对象）
- `utils.js`：删除 `formatCompact`（含测试零引用）
- `settings-connection.mjs`：删除 `MIMO_PRESET`；`settings-modal.js:12` 重导出移除该项（保留 `formatConnectionStatus`/`submitModelConnectionTest`——app-shell-static.test.mjs:99 结构断言要求）
- `cost-panel.js`：删除只写不读的 `SPARK_HEIGHT`
- `icons.js`：删除从未使用的 `plugin`/`clock`/`spark` 图标
- `connection-memory.mjs`：删除 `isModelTestedOk`/`forgetTestedModel`（listTestedModels 不调它们）+ 裁剪 `tests/app-shell/connection-memory.test.mjs` 对应用例
- `command-registry.mjs`：删除 `unregisterCommand` + 裁剪 `tests/app-shell/command-registry.test.mjs` 2 个用例
- `app.js:1-2`：删除未使用 import `deriveFailures`、`renderFailureCard`

### 5. 脚本与配置
- 删除 `scripts/create-shortcut.ps1`（用户确认；全仓零引用）
- `scripts/verify-chat-online.mjs:11`：删除未使用 import `fileURLToPath`
- package.json：移除 `dependencies.gsap`（从未被 import，项目用 `src/app-shell/vendor/gsap.js` 副本；`npm uninstall gsap` 同步更新 lockfile）
- `README.md`/`README.en.md`：更新过时的测试数量（当前写的 54 个，实际远多；以 npm test 实际输出为准）

### 6. 明确不做（有理由，不列在改动中）
- `reviewer-agent.mjs`：**保留**——被 verify-app-shell / verify-fault-injection 依赖
- 重复助手合并：`isJobRunning` 三份（tools-control 注释明确"复制避免循环依赖"）、`ensureTrailingSlash` 两份（浏览器/Node 跨层）、`parseToolCallArguments` 两份（fallback 行为略异）、`summarize` 两份（截断长度 500/4000 语义不同）、`stripMarkdown` 两份（字数统计 vs 文本清洗，语义不同）、`readOptional*` 三份（错误处理策略略异）——合并收益低、风险高
- `agent-truth.mjs` 恒空字段 `segCurrent`/`segTotal`/`etaMs`：activity-strip.js 有防御性消费，属预留契约
- `scripts/screenshot-app.cjs`、`measure-auxiliary-share.mjs`：保留（用户确认的手工工具）

## 验证
1. `npm test` 全绿（删除用例后总数略低于当前 1013，以实际为准）
2. `npm run verify:app-shell`（app-shell 改动面）
3. `npm run verify:mvp` + `npm run verify:faults`（核心路径回归；faults 依赖的 reviewer-agent 未动）
4. 不跑 Electron 类 verify（本次不涉及 preload/titlebar/harness）

## 提交（master 上直接提交，3 个 commit，中文描述）
1. `refactor: 删除核心层死代码与未使用导入`（含对应测试裁剪）
2. `refactor: 删除 app-shell 层死代码与未使用导入`（含对应测试裁剪）
3. `chore: 移除未使用的 gsap 依赖并更新 README 测试数量`（含脚本清理）