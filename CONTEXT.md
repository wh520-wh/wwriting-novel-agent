# WWriting

WWriting 是以对话为唯一控制面的本地小说 Agent。作者通过同一个对话面选择运行边界并提出写作任务，Agent 负责规划和执行项目操作。

## 对话控制面

**权限模式**：
作者为 Agent 设定的操作授权边界，决定哪些操作自动执行、哪些操作必须确认。
_Avoid_: 工作流、写作模式

**模型配置**：
保存在本机、可被作者选择的一组模型连接信息与能力信息。一个模型配置代表一个可实际调用的模型，而不只是模型名称。
_Avoid_: 供应商、模型模式

**思考强度**：
作者对模型推理投入程度的偏好，使用低、中、高等稳定档位表达；不支持该能力的模型不应伪装成已生效。
_Avoid_: 工作流、推理内容、思维链

**工作流**：
Agent 根据当前任务选择的内部执行政策，用于约束可用工具和完成条件；它不是作者在对话框中手动选择的模式。
_Avoid_: 权限模式、思考强度

**任务计划**：
Agent 为当前任务维护的有序、可验证步骤及其状态，是执行进度的公开摘要，不包含私有推理过程。
_Avoid_: 思维链、活动记录

**活动记录**：
Agent 执行过程中产生的可验证操作事实，例如读取某个文件或运行某条命令。
_Avoid_: 任务计划、私有推理
